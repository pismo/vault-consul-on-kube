kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: consul-2-pvc
  namespace: consul
  labels:
    type: amazonEBS
spec:
  storageClassName: ebs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi